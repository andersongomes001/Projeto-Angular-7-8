import { PrimeiropipePipe } from './primeiropipe.pipe';

describe('PrimeiropipePipe', () => {
  it('create an instance', () => {
    const pipe = new PrimeiropipePipe();
    expect(pipe).toBeTruthy();
  });
});
